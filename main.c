#include <stdio.h>

int maxProfit(int max_times, int price_a[], int length);

int main() {
	int price_a[] = {1, 3};
	maxProfit(0, price_a, sizeof(price_a) / sizeof(int));
	return 0;
}
/*








































*/