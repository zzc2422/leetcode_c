const int* _find(const int* left_p, int* right_p, int to_find) {
	
}

int* twoSum(const int a[], int length, int target, int* two) {
	*two = 2;
	for (const int* p = a; p != a + length; p++) {
		int value = *p, diff = target - value;
	}
}